const num1 = 20
const num2 = '20'

/**
 * == (Comparador no estricto)
 * === (Comparador estricto)
 */

if(num1 === num2) {
    console.log('Si, son iguales');
} else {
    console.log('No, no son iguales');
}